# Cloudflare Pages routing rules.
#
# Format: <from> <to> <status>
# Status 200 = rewrite (URL bar unchanged)
# Status 301/302 = redirect (URL bar changes to `to`)

# (currently no rules — seller pages are linked directly as /seller?handle=X)
